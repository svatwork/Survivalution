using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Mutations : MonoBehaviour
{
    //public Text MutationButton1;
    //public Text MutationButton2;
    //public Text MutationButton3;

    void Awake()
    {
        // Initialize mutations
    }
    
    // Start is called before the first frame update
    void Start()
    {
        // Assign random mutations to the buttons
        //MutationButton1.text = "Mutation 1";
        //MutationButton2.text = "Mutation 2";
        //MutationButton3.text = "Mutation 3";
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
